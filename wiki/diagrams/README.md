# diagrams/ — visual-only pages

Presentation tier. Mermaid is the default living diagram (text, version-controlled). Excalidraw PNGs only for hero images, kept in [png/](png/).

- [sync-flow.md](sync-flow.md) — upstream sync flow, themed (fetch=lookup · re-apply=action · conflict=guard/reject)
- `png/` — hero PNG exports (currently empty)
